# 진솔 — 경력기술서

**직무** 백엔드 · 풀스택 개발자
**경력**
- 법무법인 법승 · 2026-01-19 – 현재
- 리얼타임테크 · 공간정보융합팀 · 인턴(2024-08–2025-02) → 사원(2025-03–2025-06)

**핵심 기술 스택**
- Backend: Node.js(Express), Python(FastAPI, SQLAlchemy), TypeScript(NestJS)
- Frontend: React(Vite, TanStack Query/Table), Flutter(Dart), Next.js
- Database: PostgreSQL(Cloud SQL), MySQL, `postgres_fdw`
- Infra: GCP Cloud Run/Cloud Build/Cloud SQL/Secret Manager
- 연동: Google Workspace API, NCP SENS
- (이전 경력) Go, Spring Framework, FFmpeg, Linux

---

# 법무법인 법승 (2026-01-19 – 현재)

## 사내 사건관리 앱 — 2인 팀 개발

**기간** 2026.06 – 진행 중
**기술스택** FastAPI(Python), React/Vite/TS, PostgreSQL(postgres_fdw), GCP Cloud Run

상담료 · 수발신 문서대장 · 전자결재 · 열람등사 · 계약/미수금 관리 사내 도구. 2인 팀 개발이며 상담 · 문서 · 전자결재 · 정산 도메인과 인프라/배포를 담당.

**핵심 업무**
- FDW 원격 조인 병목 제거로 응답시간 **8초 → 0.05초(약 160배)** 개선, 타입 불일치 인덱스 복구로 **1695ms → 109ms(약 15배)** 개선
- SQLAlchemy 세션 훅(before_flush/after_flush/after_commit)으로 감사로그와 실시간 이벤트를 엔드포인트 수정 없이 전역 구현, Postgres `LISTEN/NOTIFY` 기반 SSE로 폴링 대비 추가 인프라 비용 0으로 실시간 동기화
- Gmail/Drive 기반 전자결재 시스템 구축(서명토큰 원클릭 결재, Drive 라벨 배지 동기화) — AMP 인이메일 결재는 DKIM/DMARC 미비 확인 후 롤백 판단
- 열람등사 도과 알림 자동화(공공데이터포털 특일정보 API + APScheduler)
- Cloud Run 배포 사고 2건(27분/9분 다운타임)의 근본 원인을 분석해 "빌드·배포 2단계 분리" 원칙으로 재발 방지, 이후 타 시스템에도 이식
- 회계 데이터 정합성 감사로 DB 미수금 중복 27쌍(43,818,000원) 발견

---

## 법무법인 홈페이지 V2 리뉴얼 — 1인 풀스택

**기간** 2026.05 – 진행 중
**기술스택** Next.js(App Router), NestJS, Prisma, PostgreSQL, Turborepo, GCP Cloud Run/Cloud Build/GCS

레거시 ASP.NET/SQL Server 홈페이지를 Next.js로 전면 리뉴얼. 공개 사이트 + 어드민 CMS를 스크래치부터 1인 풀스택으로 구축.

**핵심 업무**
- 레거시 SQL Server → PostgreSQL **730만 건** 데이터 이관
- 상담 접수 API `Promise.all` 병렬 처리 + fire-and-forget으로 콜드스타트 대기 **4초 제거**
- SEO/AEO: 레거시 주소 301 이전, 2024년 옛 주소 **2,328건** 정적 sitemap 제거·동적 통합, LegalService JSON-LD sameAs 주입 및 저자 Person 귀속으로 AI 답변엔진 엔티티 혼동 대응
- 보안: 상담 PII 마스킹, 전역 보안 헤더, reCAPTCHA v3 fail-closed 전환, 어드민 Google 로그인 일원화
- CMS: 예약 발행, 이미지 드래그 업로드, 자동 TOC, PDF 첨부, CSV 다운로드(마스킹)
- Cloud Build 업로드 용량 **7.6GB → 115MB**, Kaniko→BuildKit 빌드 단축, ALB+Certificate Manager로 무중단 도메인 컷오버

---

## 사내 CRM — 1인 풀스택

**기간** 2026.01 – 진행 중 (정식 오픈 2026.05)
**기술스택** Node.js(Express), Flutter(PWA+Android), PostgreSQL(Cloud SQL), Google Workspace API

전국 10개 지사의 고객 유입–변호사 배정–상담 예약–문자 안내를 통합 관리하는 CRM. 스크래치부터 1인 풀스택 구축 (**API 54개, 16개 테이블, 약 49,000 LOC**).

**핵심 업무**
- Google Sheets(Apps Script) → Cloud SQL → PostgreSQL 데이터 이관 전 과정 단독 수행(1차 적재 약 9.8만 건)
- 서브쿼리(페이지당 최대 480개) → LEFT JOIN 통합, `status`·`created_at` 복합 인덱스 추가, 조회/카운트 쿼리 `Promise.all` 병렬 처리로 목록 조회 최적화
- Cloud Run 다중 인스턴스 스케줄러 중복 실행을 추가 인프라 없이 **MySQL Named Lock(`GET_LOCK`)** 으로 해결
- 동시 접수 처리 충돌 방지를 위한 Claim(점유) 기반 동시성 제어(10분 타임아웃 자동 회수)
- Google Calendar/Admin Directory/People API 연동 자동화, NCP SENS 바이트 기준 SMS/LMS/MMS 자동 판정 + 예약·템플릿 시스템

---

## 법무법인 법승 공식 홈페이지(레거시) 유지보수

**기간** 2026.01 – 진행 중
**기술스택** ASP.NET(CSHTML), Vanilla JS/CSS, Windows Server

외주 유지보수 체계를 사내로 전환해 레거시 프론트엔드 반응형 버그 수정과 .NET 백엔드 로직을 풀스택으로 전담, FileZilla로 직접 빌드·배포. 외주 비용 절감 및 대응 리드타임 단축.

---

# 리얼타임테크 (공간정보융합팀)

## ETRI — CCTV 실시간 모니터링 웹서비스 유지보수

**기간** 2025.03 – 2025.06 (사원) · **기술** Node.js, FFmpeg, Linux(Ubuntu)

공사 현장 CCTV 스트림 웹 모니터링 시스템 유지보수. FFmpeg 스트림 처리를 Windows/Ubuntu 환경별로 분기 적용, Ubuntu 환경에서 리눅스 스크립트로 실행·배포.

## KOGAS — 신규 개발을 위한 기존 시스템 코드 분석

**기간** 2025.03 – 2025.06 (사원) · **기술** Go(gin-gonic), JavaScript(CommonJS)

가스 점검 이벤트 관리 앱의 신규 개발에 앞서, 기존 Go 백엔드/JS 프론트엔드 시스템의 API 흐름·인증 구조(JWT+API-Key+토큰 화이트리스트)·WebSocket 기반 실시간 브로드캐스트 구조를 분석해 신규 개발 설계 근거자료로 정리.

## Jubix — 공장 센서데이터 웹 유지보수·UI 개선

**기간** 2024.10 – 2025.02 (인턴) · **기술** Spring Framework, PostgreSQL, JavaScript, CSS

공장 현장 센서데이터(온도·습도·풍향) 시각화 웹 시스템의 데이터 출력·가공 로직 수정 및 UI 개선, 레거시 코드 리팩토링.
