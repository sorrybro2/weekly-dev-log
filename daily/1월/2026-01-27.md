# 일일 업무 보고 (2026. 01. 27)

> CRM (고객관계관리 시스템) — 금일 커밋 2건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **프로젝트 최초 구성**: 백엔드(Node.js)·프론트엔드(Flutter) 초기 코드베이스 커밋
- **저장소 구조 정리**: 백엔드·프론트엔드를 서브모듈에서 일반 폴더로 전환해 단일 저장소로 통합

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### 🏗️ 프로젝트 초기 구성

**최초 커밋 및 코드베이스 등록** — `README.md`, `backend/`, `frontend/`
- CRM 프로젝트의 최초 커밋으로 백엔드와 프론트엔드 코드베이스를 저장소에 등록. (`a23818c`)

**서브모듈 → 일반 폴더 구조 전환** — `backend/`, `frontend/`, `.gitignore`
- 백엔드·프론트엔드가 별도 서브모듈로 분리되어 있어 클론·이력 관리가 번거롭던 구조를, 두 디렉터리를 일반 폴더로 편입해 단일 저장소로 통합. (`d04e5e1`)
- 이 전환으로 백엔드 전 영역이 저장소에 실체로 포함됨: 인증·직원·의뢰인(party)·의뢰인 상담·SMS/통화 관리 등의 컨트롤러·라우트(`backend/src/controllers/*`, `backend/src/routes/*`), 인증 미들웨어(`backend/src/middlewares/authMiddleware.js`), DB 모델(`backend/src/models/db.js`), 시크릿 설정(`backend/src/config/secrets.js`), NCP SMS·랜덤키 유틸(`backend/src/utils/ncpSms.js`, `randomKey.js`), 앱 엔트리(`backend/src/app.js`, `backend/index.js`)와 패키지 정의(`backend/package.json`).
- 프론트엔드 Flutter 앱도 동일하게 편입: 메인 엔트리(`frontend/lib/main.dart`), 로그인·홈·상세·추가 화면과 직원·의뢰인·상담·SMS/통화 목록/상세/추가 화면들(`frontend/lib/pages/*`), 인증 서비스·가드(`frontend/lib/services/auth_service.dart`, `frontend/lib/widgets/auth_guard.dart`), 사이드 메뉴·모바일/웹 유틸(`frontend/lib/side_menu.dart`, `frontend/lib/utils/*`), Android·macOS 빌드 구성과 패키지 정의(`frontend/pubspec.yaml`).

---

**금일 종합:** CRM 프로젝트의 최초 커밋으로 백엔드·프론트엔드 코드베이스를 등록하고, 서브모듈 구조를 일반 폴더로 전환해 단일 저장소로 통합함으로써 이후 개발의 기반을 마련함.
