# 일일 업무 보고 (2026. 05. 18)

> CRM (고객관계관리 시스템) — 금일 커밋 14건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **단체 문자 발송**: 수신번호 다건 입력 UI, 묶음 식별용 batch_id 컬럼·인덱스, 발송 정보 패널 스크롤까지 단체 발송 기능 구성
- **회생 상담일지**: PNG 저장 기능 추가, 상세→수정 화면 바로 진입, 상태 필터 가로 스크롤, 유입경로 확장·자산 입력 정비, 등록일시 로컬 시간 표시
- **상담 화면**: 지사 목록의 제주지사 하드코딩 제거
- **저장소 정비**: Flutter 자동 생성 파일 추적 제외·줄바꿈 LF 고정, 릴리즈 노트 추가

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### ✉️ 단체 문자 발송 기능

**단체 발송·다건 입력 UI** — `backend/src/controllers/partySmsCallController.js`, `frontend/lib/pages/sms/sms_form_screen.dart`
- 단체 문자 발송을 지원하고 발송 정보 패널에 스크롤을 적용(`f853970`), 수신번호를 여러 건 입력하는 UI와 발송 정보 패널 스크롤을 보강(`c9dfb8e`).

**발송 묶음 식별용 batch_id** — `backend/migrations/add_batch_id_to_party_sms_call.sql`, `backend/scripts/run-sms-batch-id-migration.js`
- 단체 문자 발송을 하나의 묶음으로 식별하기 위한 batch_id 컬럼을 추가하고(`d190fda`), 해당 컬럼·인덱스 마이그레이션 실행 스크립트를 추가(`efe62e7`).

**릴리즈 노트** — `frontend/lib/pages/notices/notice_data.dart`
- 단체 문자 발송 기능에 대한 릴리즈 노트를 공지에 추가. (`71bbd6e`)

#### 📄 회생 상담일지 개선

**PNG 저장 기능** — `frontend/lib/pages/recovery/recovery_intake_result_screen.dart`, `frontend/lib/pages/recovery/services/pdf_download_helper*.dart`, `recovery_intake_pdf_service.dart`, `frontend/lib/pages/recovery/widgets/recovery_intake_png_renderer.dart`, `frontend/web/index.html`
- 회생 상담일지 결과 화면에서 PNG로 저장하는 기능을 추가. (`4f3dee1`)

**입력·조회 편의** — `frontend/lib/pages/recovery/recovery_intake_form_screen.dart`, `recovery_intake_result_screen.dart`, `recovery_intake_screen.dart`
- 회생 상담 상세에서 바로 수정 화면으로 진입하도록 지원(`963e11f`), 상태 필터를 가로 스크롤로 변경해 모바일 화면을 정리(`06f5bf5`).
- 유입경로 선택지를 확장하고 자산 입력 힌트를 정비(`419eeb0`), 자산 입력 기본값 `×`를 제거해 빈 입력으로 시작하도록 변경(`09358f4`), 상담 목록 등록일시를 로컬 시간으로 표시(`89ce4e1`).

#### 📞 상담 화면 정비

**제주지사 하드코딩 제거** — `frontend/lib/pages/consultation/consultation_screen.dart`
- 상담 화면 지사 목록에서 제주지사 하드코딩을 제거. (`78354ca`)

#### 🗂️ 저장소 정비

- **자동 생성 파일 추적 제외** — `frontend/.gitignore`, 각 플랫폼 `generated_plugin_registrant`/`generated_plugins`: Linux·macOS·Windows 자동 생성 플러그인 파일을 추적에서 제외. (`4a94c52`)
- **줄바꿈 LF 고정** — `.gitattributes`: Flutter 생성 폴더의 줄바꿈을 LF로 고정. (`a1f007e`)

---

**금일 종합:** 단체 문자 발송 기능을 다건 입력 UI·batch_id 식별·패널 스크롤까지 구성하고, 회생 상담일지에 PNG 저장·수정 진입·필터/입력 편의를 더했으며, 상담 화면 하드코딩 제거와 저장소 자동 생성 파일 정비를 병행함.
