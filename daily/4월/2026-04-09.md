# 일일 업무 보고 (2026. 04. 09)

> CRM (고객관계관리 시스템), Cloud Run 인프라 — 금일 커밋 16건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **성능 개선**: 당사자·상담 목록 조회 속도 개선 및 DB 인덱스 추가
- **시간대 정정**: 문자 발송 일시·예약 발송 시간을 한국 시간으로 표시(UTC 오해석 수정)
- **환경 분리**: 테스트/운영 서버 설정·웹 주소·로그인·API 연결 분리
- **당사자 시트 동기화**: 시트 수정 내용 DB 즉시 반영, 시간 9시간 밀림 수정, 낡은 Apps Script 제거
- **연락처·삭제**: 지사 공용계정으로 연락처 저장, 당사자 삭제·목록 갱신·필터 갱신 수정

**[Cloud Run 인프라]**
- **시트 동기화**: 당사자 삭제 시 구글 시트 행도 함께 삭제

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### ⚡ 성능 개선

**목록 조회 속도 개선 및 인덱스** — `backend/src/controllers/partyController.js`, `partyConsultationController.js`, `backend/migrations/add_performance_indexes.sql`
- 당사자·상담 목록 조회 쿼리를 개선하고(`e8d5ad5`, `6122d2f`, `5c09475`), 당사자 목록 조회 속도를 위한 DB 인덱스를 추가. (`16a8d8e`)

#### 🕐 시간대 정정

**문자 발송 시각 한국 시간 표시** — `backend/src/controllers/partySmsCallController.js`, `frontend/lib/pages/sms/sms_history_screen.dart`
- 문자 발송 일시를 한국 시간으로 표시하고(`4451244`), 예약 발송 시간도 한국 시간으로 표시(`fb94782`)하며, 발송 시각이 UTC로 잘못 해석되던 문제를 수정. (`dc323cb`)

#### 🔀 환경 분리

**테스트/운영 서버 설정 분리** — `.gitignore`, `backend/package.json`, `backend/src/app.js`, `backend/src/config/loadEnv.js`, `secrets.js`, `backend/src/controllers/authController.js`
- 테스트 서버와 운영 서버 설정을 분리 지원해 환경별 구성을 안전하게 관리. (`44f43fe`)

#### 🔄 당사자 시트 동기화

**시트 수정 내용 DB 즉시 반영** — `backend/scripts/apps-script/partySheetSqlWebhook.gs`, `backend/src/controllers/partySheetWebhookController.js`, `backend/src/routes/party.js`, `backend/src/services/partySheetSqlSyncService.js`
- 구글시트에서 수정한 내용을 당사자 DB에 바로 반영하도록 지원. (`e13fcce`)
- 당사자 시트 시간값이 9시간 밀리는 문제를 수정하고(`58e261e`), 낡은 당사자 시트 웹훅 Apps Script 파일을 제거. (`3b38748`)

#### 👤 연락처·삭제·필터

- **지사 공용계정 연락처 저장** — `backend/src/controllers/googleContactController.js`, `partyCreateController.js`, `partyUpdateController.js`, `backend/src/services/googleContactTargetService.js`, `partyGoogleContactAutoSyncService.js`: 선택한 지사 공용계정으로 연락처를 저장하도록 수정. (`f3661a3`)
- **당사자 삭제 안정화** — `backend/src/models/db.js`: 당사자 삭제가 실패하지 않도록 수정. (`781cf45`)
- **삭제 후 목록 갱신** — `frontend/lib/pages/party/party_detail_screen.dart`, `party_form_screen.dart`, `router/app_router.dart`: 당사자 삭제 후 목록이 바로 갱신되도록 수정. (`20a64c7`)
- **필터 즉시 갱신** — `frontend/lib/pages/consultation/consultation_screen.dart`, `pages/party/party_screen.dart`: 필터 적용 직후 목록이 바로 갱신되도록 수정. (`84ae4c9`)

### 2. Cloud Run 인프라

#### ☁️ 시트 동기화 보완

**당사자 삭제 시 시트 행 삭제** — `party-sheet-sync/src/index.js`
- 당사자 삭제 시 구글 시트의 해당 행도 함께 삭제되도록 동기화 서비스를 보완. (`b07a1c5`)

---

**금일 종합:** 목록 조회 성능과 인덱스, 시간대 표시, 테스트/운영 환경 분리, 당사자 시트 양방향 동기화까지 광범위하게 정비하고, Cloud Run 시트 동기화 서비스에도 삭제 반영을 보완해 데이터 정합성을 크게 높임.
