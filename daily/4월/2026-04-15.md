# 일일 업무 보고 (2026. 04. 15)

> CRM (고객관계관리 시스템) — 금일 커밋 17건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **역방향 캘린더 동기화**: 구글 캘린더 수정·삭제를 CRM 상담·일정 테이블에 역방향 반영(웹훅), 이후 역방향 진입점은 비활성화·기존 편집 화면으로 통일
- **캘린더 일정 편집**: 방문·전화(변호사)·접견 상담을 캘린더 일정 편집 화면으로 이동, 조회·수정 API 및 전용 테이블 추가, CRM 상담 상세 하이퍼링크·'CRM' 표시
- **상담 사이드 패널**: 상담 목록에서 사이드 패널로 상세·등록 지원
- **직원 동기화**: Admin 직원 추가 항목을 SQL 직원 정보에 함께 저장
- **보안/문서**: 운영 로그에 인증 토큰 미노출, 스키마·ERD 문서 최신화

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### 🔄 역방향 캘린더 동기화

**구글 캘린더 → CRM 역방향 반영** — `backend/migrations/create_google_calendar_watch_channel_table.sql`, `backend/src/app.js`, `backend/src/routes/index.js`, `backend/src/services/googleCalendarPushSyncService.js`
- 구글 캘린더 수정·삭제 시 CRM 상담에 역방향으로 반영하는 웹훅 동기화를 구축하고(`5af20ba`), 변경을 상담 일정 테이블까지 반영. (`27fe11f`)
- watch 위임 이메일을 `GOOGLE_CALENDAR_DELEGATE_EMAIL`로 변경(`a45b146`), 웹훅 URL 직접 지정 환경변수 추가(`87a8c41`), 웹훅 수신 여부를 로그로 확인 가능하게 처리. (`b00fcc8`)
- 이후 역방향 캘린더 동기화·편집 진입점은 비활성화하고 기존 편집 화면으로 통일. (`ff54e64`, `edcd30d`)

#### 📅 캘린더 일정 편집

**상담 → 캘린더 일정 편집 화면 통일** — `frontend/lib/pages/consultation/consultation_form_screen.dart`, `consultation_schedule_form_screen.dart`, `consultation_screen.dart`, `services/consultation_form_service.dart`, `router/app_router.dart`, `backend/src/controllers/partyConsultationController.js`, `backend/src/routes/partyConsultation.js`, `backend/migrations/create_consultation_schedule_table.sql`
- 방문·전화(변호사)·접견 상담 선택 시 캘린더 일정 편집 화면으로 이동하도록 하고(`c935940`), 캘린더 상담 일정 조회·수정 API(`43a4ed8`)와 전용 테이블 마이그레이션(`396c0dc`)을 추가.
- 상담 등록·수정·삭제 시 consultation_schedule을 자동 동기화하고(`df4ee00`), 캘린더 타입 상담의 편집 진입점을 상세 → 편집 버튼으로 통일. (`9c0185d`)
- 일정 편집 폼의 상담 내용 필드 레이블을 DB 컬럼명과 일치시킴. (`3233201`)

**캘린더 하이퍼링크·표시** — `backend/src/controllers/partyConsultationController.js`, `backend/src/services/googleCalendarService.js`
- 구글 캘린더 일정 내용 상단에 CRM 상담 상세 하이퍼링크를 추가하고(`6e79aca`), CRM에서 생성·수정한 일정 제목 끝에 'CRM' 표시를 추가. (`a951f2e`)

#### 🧩 상담 사이드 패널

**상담 목록 사이드 패널** — `frontend/lib/pages/consultation/consultation_form_screen.dart`, `consultation_screen.dart`
- 상담 목록에서 사이드 패널로 상담 상세·등록을 지원해 화면 전환 없이 처리하도록 개선. (`1c91b52`)
- 당사자 상담 이력 카드의 아이콘·색상을 개선하고 예약 배지를 노란색으로 변경. (`d65e256`)

#### 👥 직원 동기화 · 보안 · 문서

- **Admin 직원 추가 SQL 반영** — `backend/database/create_employee_tables_simple.sql`, `create_missing_tables.sql`, `backend/src/services/employeeAdminEmpInfoSyncService.js`, `employeeImmediateGoogleSyncService.js`, `employeeMasterSyncService.js`: Admin 직원 추가 항목이 SQL 직원 정보에 함께 저장되도록 반영. (`eef8a7b`)
- **인증 토큰 로그 미노출** — `backend/src/middlewares/authMiddleware.js`: 운영 로그에 인증 토큰이 남지 않도록 처리. (`9c2b32f`)
- **Railway 전용 코드 제거** — `backend/src/app.js`: 사용하지 않는 Railway 전용 코드를 제거. (`b17fbd1`)
- **스키마·ERD 문서 최신화** — `README.md`, `backend/README.md`, `backend/database/bs_erd.md`, `backend/scripts/generate-cloudsql-mermaid-erd.js`: 캘린더 동기화 스키마 문서를 최신화. (`9a51a17`)

---

**금일 종합:** 구글 캘린더 역방향 동기화 웹훅을 구축·검증한 뒤 편집 진입점을 단일 화면으로 통일하고, 캘린더 일정 편집 API·전용 테이블·하이퍼링크와 상담 사이드 패널까지 갖춰 캘린더 연동 아키텍처를 정리함. 직원 동기화·토큰 로그 위생·문서 최신화도 병행.
