# 일일 업무 보고 (2026. 02. 12)

> CRM (고객관계관리 시스템) — 금일 커밋 20건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **폰트 품질 개선**: Pretendard 폰트 적용, 한글 선명도·글자 크기 확대(+2px), 폰트 깨짐 방지
- **태그/테이블 UI**: 태그 뱃지 글꼴 색상·색상 유지 개선, 태그 색상 선택 UX 개선
- **테이블 개인화**: 테이블 컬럼 순서 계정별 저장, 문자 내역 기본 컬럼 순서 변경, 페이지당 줄개수 제한 수정
- **당사자·상담 UX**: 당사자 폼 UI·상담 이력 배치 개선, 수정 시 상담 이력 추가 팝업 지원
- **DB 안정화**: Railway MySQL 연결 끊김 대응(pool 재생성/재시도·SSL 옵션)

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### 🔤 폰트 품질 개선

**Pretendard 적용 및 한글 선명도 개선** — `frontend/lib/main.dart`, `frontend/fonts/Pretendard-*.ttf`, `frontend/pubspec.yaml`, `frontend/web/index.html`, `frontend/lib/widgets/common_data_table.dart`, `status_badge.dart`, 각 `*_screen.dart`
- **Pretendard 폰트**를 적용하고 태그·테이블 UI를 함께 개선. (`1978a7d`, `2a11e68`)
- 한글 폰트 선명도를 개선(`52d3c18`)하고, 목록 UI에서 폰트를 확대하며 스크롤을 동적 처리(`4994497`), 폰트 깨짐 방지를 위해 글씨 크기를 +2px씩 키움. (`0f31a8b`)

#### 🏷️ 태그·테이블 UI

**태그 뱃지 색상·컬럼 순서 개인화** — `frontend/lib/pages/party_form_screen.dart`, `party_detail_screen.dart`, `consultation_screen.dart`, `party_screen.dart`, `sms_history_screen.dart`, `frontend/lib/widgets/status_badge.dart`, `common_data_table.dart`
- 태그 뱃지의 글꼴 색상을 개선하고 색상이 유지되도록 처리(`408b20a`), 태그 색상 선택 UX를 개선. (`1c7721f`)
- 당사자 등록 폼의 태그·입력 필드 글자 색상을 수정. (`93a4193`)
- **테이블 컬럼 순서를 계정별로 저장**하는 기능을 추가하고(`0c50eb7`), 문자 내역 기본 컬럼 순서를 변경(`f511c20`), 테이블 한 페이지당 줄개수 제한을 수정. (`0f630ca`)

#### 👥 당사자·상담 UX

**당사자 폼·상담 이력 배치 개선** — `frontend/lib/pages/party_form_screen.dart`, `party_detail_screen.dart`, `consultation_form_screen.dart`, `sms_form_screen.dart`, `sms_history_screen.dart`, `frontend/lib/router/app_router.dart`, `frontend/lib/widgets/status_badge.dart`
- 당사자 폼 UI와 상담 이력 배치를 개선(`a65a4ed`)하고, 당사자 수정 시 상담 이력 추가 팝업을 지원. (`d331290`)
- 문자 내역 메인 및 생성 화면을 개선(`5817ecb`)하고, 목록 화면 복귀 시 항상 새로고침이 적용되도록 처리. (`e980154`)

#### 🛢️ DB 연결 안정화

**Railway MySQL 연결 끊김 대응** — `backend/src/app.js`, `backend/src/models/db.js`, `backend/README.md`, `.gitignore`
- Railway MySQL 연결 끊김에 대응해 pool 재생성/재시도 및 SSL 옵션 지원을 추가하고 반복 보강. (`e8db5a7`, `356ffce`, `803ff3c`, `ca1f4cd`, `679eee1`)

---

**금일 종합:** Pretendard 폰트 적용과 한글 선명도·글자 크기 조정으로 전반 가독성을 끌어올리고, 태그 뱃지·테이블 컬럼 순서의 계정별 개인화를 강화했다. 당사자 폼·상담 이력 UX를 다듬고 Railway MySQL 연결 끊김에 대한 pool 재시도·SSL 대응으로 DB 안정성을 확보함.
