# 일일 업무 보고 (2026. 02. 05)

> CRM (고객관계관리 시스템) — 금일 커밋 6건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **Railway·Firebase 배포 설정**: Railway 및 Firebase 배포 설정 추가, 재배포 트리거
- **DB 포트 설정**: Railway MySQL TCP Proxy 연결을 위한 port 설정 지원 추가
- **저장소 정리**: SQL 파일·임시 스크립트·빌드 캐시를 gitignore에 정리

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### ☁️ Railway·Firebase 배포 설정

**배포 설정 추가 및 재배포 트리거** — `.gitignore`, `frontend/.firebaserc`, `frontend/firebase.json`, `frontend/lib/config/api_config.dart`, `frontend/lib/pages/login_page.dart`
- Railway(백엔드)와 Firebase(프론트) 배포 설정을 추가. (`4fdb3e8`)
- 포트 설정 반영 및 배포 상태 확인을 위한 **재배포 트리거** 커밋을 수행. (`8f74ef0`, `9174549`)

**MySQL 포트 설정 지원 추가** — `backend/src/models/db.js`
- Railway의 MySQL TCP Proxy 연결을 위해 접속 **port 설정을 추가**하고, `DB_PORT` 환경 변수를 지원하도록 개선. (`9e09e5f`)

#### 🧹 저장소 정리

**빌드 캐시·SQL 임시 파일 gitignore 정리** — `frontend/.gitignore`
- Firebase 빌드 캐시(`55a5bb7`)와 SQL 파일·임시 스크립트(`e951f3b`)를 gitignore에 추가해 불필요한 파일이 저장소에 포함되지 않도록 정리.

---

**금일 종합:** Railway·Firebase 배포 파이프라인 설정을 추가하고 MySQL 포트 연결을 지원하도록 보완했으며, 빌드 캐시와 임시 SQL 파일을 gitignore로 정리해 저장소 위생을 확보함.
