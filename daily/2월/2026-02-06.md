# 일일 업무 보고 (2026. 02. 06)

> CRM (고객관계관리 시스템) — 금일 커밋 15건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **구글 OAuth 도메인 제한**: 로그인·토큰 검증에 도메인 계정 제한을 적용해 사내 계정만 접근하도록 보안 강화
- **Railway 배포 연동**: 프론트 백엔드 URL을 Railway 주소로 전환, 외부 접근 바인딩(0.0.0.0) 수정, 배포 설정 추가
- **DB 테이블명 정합화**: `party` 참조를 실제 Railway DB 구조인 `party_detail`로 전면 수정
- **저장소 정리**: SQL 덤프·변환 스크립트를 archive 폴더로 이동 및 gitignore 정리

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### 🔐 구글 OAuth 도메인 로그인 제한

- **OAuth 도메인 제한 적용** — `backend/src/controllers/authController.js`: 구글 OAuth 로그인 시 허용 도메인을 제한해 사내 계정만 접근 가능하도록 제한. (`94e0658`)
- **토큰 검증 도메인 제한** — `backend/src/app.js`: 토큰 검증 API에도 도메인 제한을 적용해 인증 경로를 이중으로 잠금. (`bd82821`)
- **로그인 화면 도메인 계정 유도** — `frontend/lib/pages/login_page.dart`: 로그인 화면에서 도메인 계정으로 로그인하도록 안내·유도. (`a4b5188`)

#### ☁️ Railway 배포 연동

**백엔드 URL 전환 및 외부 접근 바인딩** — `frontend/lib/config/api_config.dart`, `backend/src/app.js`, `backend/nixpacks.toml`, `backend/README.md`
- 프론트엔드의 백엔드 API URL을 Railway 프로덕션 주소(BS_CRM)로 전환. (`7006881`, `44f027b`, `70894d1`)
- Railway 외부 접근을 허용하기 위해 서버 바인딩을 **0.0.0.0**으로 수정해 모든 인터페이스에서 접근 가능하도록 처리. (`d1eabcf`, `22d02fa`, `be143d2`)
- Railway 배포 설정(nixpacks, README, database 스키마 파일)을 추가·정리. (`3dd595e`, `917434a`)

#### 🗂️ DB 테이블명 정합화

**`party` → `party_detail` 참조 수정** — `backend/src/controllers/partyController.js`, `partyConsultationController.js`, `partySmsCallController.js`, `employeeController.js`
- Railway DB의 실제 테이블 구조에 맞춰 `party` 테이블 참조를 **`party_detail`** 로 일괄 수정. SELECT·INSERT·JOIN·서브쿼리 전반의 테이블명을 정합화해 조회 오류를 해소. (`2b7cdbd`, `03f7aa7`)

#### 🧹 저장소 정리

**SQL 덤프·변환 스크립트 정리** — `.gitignore`
- Railway DB import 작업 후 남은 SQL 덤프 및 임시 변환 스크립트 35개를 archive 폴더로 이동하고, gitignore를 간소화. (`fbb736f`, `cfc77a7`)

---

**금일 종합:** 구글 OAuth 도메인 제한으로 사내 계정 전용 접근을 확보하고, 백엔드를 Railway로 전환하며 외부 접근 바인딩과 배포 설정을 정비했다. 아울러 DB 테이블 참조를 실제 구조(`party_detail`)에 맞게 정합화하고 임시 SQL 파일을 정리해 배포 안정성을 높임.
