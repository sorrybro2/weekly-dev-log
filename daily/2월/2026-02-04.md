# 일일 업무 보고 (2026. 02. 04)

> CRM (고객관계관리 시스템) — 금일 커밋 5건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **배포 환경 구성**: Docker 설정 추가 및 프로덕션 배포 환경 구성, start 스크립트·service-account 처리 정비
- **DB 접속 환경변수화**: MySQL 접속 정보를 환경변수로 전환
- **문서화**: README에 시스템 구성도·DB 구조 섹션 정리

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### 🐳 프로덕션 배포 환경 구성

**Docker 및 배포 환경 구성** — `backend/Dockerfile`, `backend/.dockerignore`, `backend/src/app.js`, `frontend/lib/config/api_config.dart`
- 프로덕션 배포를 위한 Docker 설정을 추가하고 배포 환경을 구성. (`ef66aa1`)
- 배포 시 구동에 필요한 `start` 스크립트를 `package.json`에 추가하고(`24adfeb`), 배포 환경에서 없을 수 있는 `service-account.json`을 필수가 아닌 **optional로 변경**해 기동 실패를 방지. (`9e142a1`)

#### 🔧 DB 접속 정보 환경변수화 및 문서 정비

**DB 접속 환경변수 전환 및 README 정리** — `backend/src/models/db.js`, `README.md`
- `db.js`의 MySQL 접속 정보를 하드코딩 대신 **환경변수(DB_HOST, DB_USER, DB_PASSWORD, DB_NAME)** 로 변경하고, README의 데이터베이스 구조를 실제 테이블/컬럼 기준으로 수정. (`6009f1b`)
- README에 시스템 구성도 섹션을 추가하고 목차를 정리. (`de419e9`)

---

**금일 종합:** CRM 백엔드의 프로덕션 배포 준비 작업으로 Docker 환경을 구성하고, DB 접속 정보를 환경변수로 분리해 배포 유연성을 확보하며 README 문서를 실제 구조에 맞게 정비함.
