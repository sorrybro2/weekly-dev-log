# 일일 업무 보고 (2026. 08. 26.)

> 마케팅 요청으로 시작한 **검색 노출 문구 개편**이 지사 제목까지 번졌다가, 요청 범위를 넘어선 판단이었음을 스스로 인지해 **9분 만에 되돌린** 하루였다. 같은 흐름에서 홈 변호사 카드와 변호사 상세 검색 설명문을 지배하던 정형문(`○○ 변호사는 법무법인 법승 ○○분사무소의 변호사입니다`)을 걷어냈고, 자동 선별 알고리즘을 만들었다가 **어드민에서 사람이 고쳐도 규칙이 이긴다**는 이유로 폐기하고 입력 순서를 그대로 쓰는 쪽을 택했다. 별건으로 홈 첫 화면이 늦게 뜨는 원인을 두 겹(발견 지연 · 원본 형식)으로 분리해 각각 조치했고, 그 과정에서 **`next/image` 변환 캐시가 Cloud Run 인스턴스마다 따로라 배포할 때마다 날아간다**는 구조를 확인해 기존 ISR 캐시 이슈와 같은 뿌리로 묶었다. 계측 쪽에서는 GTM 컨테이너 15개를 전수 파싱해 **GA4 상담 접수 전환이 전 지사 0** 이라는 것을 새로 찾았고, 오후에는 미해결 이슈 11건을 전수 점검해 3건을 종결했다 — 금일 커밋 10건(1개 repo), 이슈 코멘트 22건

## ⏱️ 10초 핵심 요약

**[홈페이지 V2 / 검색 노출 문구 개편 — 그리고 자기 범위 초과 정정]**
- **비브랜드 검색에 걸릴 문자열이 제목에 없었다**: 사이트 제목이 `법무법인 법승` 브랜드명 한 마디. 경쟁사(YK·대륜·태하·AK)는 전부 제목에 분야를 박아 둔다 → `법무법인 법승 | 형사전문변호사`
- **키워드 나열이 아니라 평서문으로**: 홈 소개글에 지역·사건 키워드를 문장 형태로. 콤마 나열은 구글이 스니펫으로 쓰지 않는다
- **AI 답변엔진이 먼저 읽는 자리까지**: 법인 JSON-LD 설명과 `llms.txt` 머리말이 '형사'를 첫 마디로 말하게 함
- **🔴 스스로 되돌림**: 지사 8곳 제목까지 코드 폴백으로 채웠다가(`5455678`) **9분 뒤 전량 revert**(`0c512f6`). 지사 문구는 마케팅·대행사 검수를 거쳐 어드민에 넣는 것이 정상 경로다

**[홈페이지 V2 / 변호사 노출 문구 — 정형문 걷어내기]**
- **32명 중 대부분이 같은 문장이었다**: 홈 카드 아래 한 줄이 지점과 직급만 말해 클릭할 이유를 주지 못했다
- **자동 선별을 만들었다가 버림**: 점수 규칙으로는 32명 중 17명만 채워지고 성격이 제각각이었으며, **결정적으로 어드민에서 문구를 고쳐도 규칙이 이겨 사람이 통제할 수 없었다**
- **채택안은 이미 있는 칸**: 어드민 `요약 경력`(32명 전원 입력됨) 앞 두 줄을 그대로. 새 칸 없이 **입력 순서 = 노출 순서**
- **검색 설명문도 같은 병**: 변호사 상세 `meta description` 과 JSON-LD 가 인사말을 먼저 써서 검색결과에 정보가 없었다 → 소개 본문(평균 137자) 우선으로

**[홈페이지 V2 / 첫 화면 성능 — 원인이 두 겹이었다]**
- **① 발견 지연**: 배경 네 장이 전부 지연 로딩이라 레이아웃을 끝낸 뒤에야 사진을 발견 → 첫 방문에서 0.5~1초 검은 판. **두 번째 방문부터 빨랐던 것이 근거**
- **② 원본 형식**: PNG 최대 5.5MB 라 `next/image` 가 요청마다 변환 → **장당 0.45~0.94초**(캐시 미스 실측). WebP 사전 변환으로 5.54MB→589KB
- **⭐ 곁가지 발견**: 그 변환 캐시는 컨테이너 파일시스템에 있다 — **Cloud Run 인스턴스마다 따로이고 배포하면 날아간다.** 배포 직후 방문자는 거의 항상 변환 대기를 만난다
- **기존 이슈와 같은 뿌리로 묶음**: ISR 캐시가 인스턴스별로 도는 `#82` 와 동일 구조

**[홈페이지 V2 / 화면 결함·내부 링크]**
- **짧은 폰에서 사례가 조용히 사라지고 있었다**: 높이 상수(`15.25rem`)가 실제와 어긋난 만큼 상담바 밑으로 들어가 **3·4번째 사례와 '더보기' 가 통째로** 잘렸다. 오류도 스크롤도 안 난다
- **🔴 지사 페이지로 가는 내부 링크가 사이트 전체에 0개**: 지역 선택이 쿠키 기반이라 URL 이 화면에 안 드러나고, 사무소 지도도 전화·지도 링크만 갖고 있었다
- **사이트맵만으로는 부족한 이유**: '존재 신고' 는 되지만 '중요도 신고' 가 안 된다. 구글은 사이트링크를 **내부 링크 구조에서** 뽑는다 → 전 페이지 공통인 푸터에 지사 링크를 세움

**[홈페이지 V2 / 전환추적 계측 감사]**
- **🔴 GA4 상담 접수 전환이 전 지사 0 — 신규 발견**: V2 이벤트를 수신하는 GA4 태그가 **13개 컨테이너에 하나도 없다.** 절반은 `gtm.formSubmit` 대기(V2 는 fetch 제출), 절반은 레거시 이벤트명과 불일치
- **CSS 선택자 기반 GA4 태그 20여 개 사망**: 반대로 URL 조건 태그는 V2 에서도 정상 발화한다 — 전멸은 아니다
- **PC 대표전화 전환이 통째로 빠져 있었다**: 잡히던 경로는 모바일 하단 독 하나뿐 → 복원(`2048bcf`)
- **정정 2건**: ① 빠진 두 자리가 둘 다 **PC 경로**였다(모바일 GNB 표기는 부정확) ② **푸터 `tel:` 은 붙이면 안 된다** — 레거시에서 푸터 전화를 전환으로 잡은 지사는 **수원 하나뿐**

**[홈페이지 V2 / 미해결 이슈 전수 점검]**
- **종결 3건**: 전문센터 전화 버튼(`#56`) · 레거시 404 URL 301 매핑(`#114`) · 모바일 검색 팝업(`#119`) — 전부 라이브 실측 후 닫음
- **🔴 지금도 진행 중인 유실 발견**(`#81`): 하단 상담바에서 '회생·파산' 을 골라도 **분기가 코드에 없다.** 회생팀 화면에 안 뜨고 웹훅도 안 울린다
- **어드민 "저장했는데 안 바뀐다"의 정체**(`#82`): 공유 캐시 핸들러가 없어 인스턴스마다 ISR 캐시가 따로 돈다 → **재현 안 되는 버그로 오인**되기 쉽다
- **성격 재분류**: 상담 캡(`#109`)은 남은 Redis 항목만 별도 이슈로 떼는 것이 맞고, CRM(`#99`)은 개발 몫 없이 회신·운영 합의만 남았다

---

## 📝 상세 업무 내역

### 1. 홈페이지 V2 — 검색 노출 문구 개편 (이슈 `#121`)

- **사이트 전역 제목·소개글** — 브랜드명 한 마디였던 제목을 `법무법인 법승 | 형사전문변호사` 로 바꾸고, 홈 소개글에 지역(서울·수원·인천)과 사건(성범죄·마약·음주운전·사기·횡령·학교폭력·행정처분) 키워드를 **콤마 나열이 아닌 평서문**으로 담았다 — 나열식은 구글이 스니펫으로 쓰지 않는다. `app/layout.tsx`·`app/(site)/page.tsx` (`98989e2`)
- **하위 6개 페이지·AI 인용 자리·공유 카드** — 소개·변호사·사례·지식센터·상담·후기의 한 줄 소개글을 키워드 문장으로 늘리고, 법인 JSON-LD 설명과 `llms.txt` 머리말이 '형사'를 첫 마디로 말하게 했다(답변엔진은 본문보다 이 자리를 먼저 인용한다). `og`·`twitter`·`manifest.json` 도 같은 문구로 동기화. (`98989e2`)
- **지사 8곳 제목 폴백 — 그리고 전량 되돌림** — 8곳 제목이 `법무법인 법승 인천분사무소` 처럼 상호+지역뿐이라 지역 질의에 걸릴 문자열이 없었다. 정비된 서울·수원과 `|` 구분자 유무로 두 상태를 가르고 안 된 쪽만 채웠으나(`5455678`), **9분 뒤 전량 revert 했다**(`0c512f6`). 요청 범위는 리뉴얼 홈(루트)이었고, 지사 문구는 지사별 주력 사건과 붙어 있어 마케팅·대행사 검수를 거쳐 어드민(`tbl_company`)에 넣는 것이 정상 경로다. `lib/region-seo.ts`
- **되돌리면서 별건 2건을 남김** — ① 서울 `og_title` 이 브랜드명 한 마디라 공유 카드 제목에 지역이 없는 상태 ② 지사 6곳 `og_desc` 가 지사 무관 일반 문구여서 지사 페이지를 공유하면 카드가 브랜드 소개로 퇴화하는 상태. **운영 조치** (`#121`)

### 2. 홈페이지 V2 — 변호사 노출 문구 (마케팅 요청 · 이슈 `#122`)

- **문제 정의** — 홈 변호사 카드(모바일) 아래 한 줄이 `tbl_lawyer.maindesc` 인데 **32명 중 대부분이 같은 정형문**이었다. 지점과 직급만 말해 클릭할 이유를 주지 못한다는 마케팅 지적이 정확했다. (`#122`)
- **자동 선별안을 만들었다가 폐기** — `carrer` 에 키워드 점수를 매겨 후킹 이력을 뽑는 방식을 실측으로 버렸다. `carrer` 는 평균 15줄에 앞부분이 거의 학력이라 앞에서 자를 수 없었고, 점수 규칙으로는 **32명 중 17명만** 채워졌으며 뽑힌 줄의 성격이 제각각이었다. 결정적으로 **어드민에서 문구를 고쳐도 규칙이 이겨 사람이 통제할 수 없었다.** (`#122`)
- **채택안 — 이미 있는 칸을 쓴다** — `tbl_lawyer.hovercarrer`(어드민 `요약 경력`)의 앞 두 줄을 그대로 노출한다. 32명 전원 채워져 있고 사람이 이미 추린 목록이라 새 컬럼·새 화면을 만들지 않았다. 점수 규칙 없이 **입력 순서 = 노출 순서**이며, 어드민 힌트에 "앞 두 줄이 메인 홈 카드에 노출된다"를 명시해 바꾸는 방법을 그 자리에서 알 수 있게 했다. `backend/src/lawyers/lawyer-highlights.ts`(신설)·`components/home/lawyers-tabs.tsx`·`lawyer-editor.tsx` (`e9a47b7`)
- **배포 중간 상태 대비** — `highlights` 를 optional 로 뒀다(전체 배포가 프론트를 먼저 띄워 몇 분간 이 필드가 없는 응답을 받는다). 두 줄을 못 채우면 기존 정형문으로 돌아가고, 카드 높이는 두 줄로 못 박아 사람이 바뀔 때 덜컥거리지 않게 했다. (`e9a47b7`)
- **검색 설명문도 같은 병이었다** — 변호사 상세의 `meta description` 과 Attorney JSON-LD 가 인사말(`maindesc`)을 먼저 써서 검색결과 설명문에 정보가 없었다. 소개 본문(`subdesc`, 32명 전원·평균 137자)을 먼저 쓰도록 바꿨다. `components/seo/attorney-jsonld.tsx`·`lib/content-seo.ts` (`a07644d`)
- **🔴 남은 일** — 32명 중 **15명은 `요약 경력` 첫 줄이 학력·소속**이다. 코드로 고칠 수 없고 어드민에서 눈에 띄는 줄을 위로 올려야 한다. **운영 조치** (`#122`)

### 3. 홈페이지 V2 — 홈 첫 화면 성능 (이슈 `#101`)

- **원인 ① 발견 지연** — 히어로 배경 네 장이 **전부 지연 로딩**이라 브라우저가 레이아웃을 끝낸 뒤에야 사진을 발견해 첫 방문에서 0.5~1초 검은 판이 머물렀다. **두 번째 방문부터 빨랐던 것이 근거**다. 지금 보이는 장에만 `priority` 를 주고, 넘김으로 올 다음 장은 첫 화면과 대역폭을 다툴 이유가 없어 지연 로딩을 유지했다. `components/home/hero-backdrop.tsx` (`ac81362`)
- **원인 ② 원본 형식** — PNG 가 세로판 5.5MB·가로판 2.0MB 라 `next/image` 가 요청마다 디코드+인코딩했고 **장당 0.45~0.94초**가 그 변환에 들어갔다(캐시 미스 실측). WebP(q90) 사전 변환으로 1.62MB→65KB, 1.99MB→158KB, **5.54MB→589KB**. 전송 품질도 92→88 로 내렸다(원본이 이미 WebP 라 92 재인코딩은 이득 없이 파일만 키웠다). PNG 는 되돌릴 자리로 남겼다. (`0fec4fc`)
- **⭐ 곁가지 발견 — 변환 캐시가 인스턴스마다 따로다** — 그 캐시는 컨테이너 파일시스템에 있어 **Cloud Run 인스턴스마다 따로이고 배포하면 날아간다.** 배포 직후·스케일아웃 직후 방문자는 거의 항상 변환 대기를 만나고, 여러 번 배포한 날에는 계속 느리게 보인다. **ISR 캐시가 인스턴스별로 도는 `#82` 와 같은 뿌리**(인스턴스 로컬 캐시를 공유 저장소로 못 빼고 있는 구조)여서 두 이슈를 연결해 기록했다. (`#101`·`#82`)
- **남은 항목** — `main_standing_people_mobile` 은 **원본이 427px 폭**이라 폰 화면에서 확대돼 흐리다. 변환으로는 못 고치고 더 큰 원본을 받아야 한다. **운영 조치** (`#101`)

### 4. 홈페이지 V2 — 홈 사례 섹션 잘림 (이슈 `#112`)

- **조용히 잘리고 있던 결함** — 장표 `03 sec-cases` 에서 **3·4번째 사례와 '더보기' 가 통째로 사라져 있었다.** 목록 높이 상수(`15.25rem`)가 실제와 몇 px만 어긋나도 그만큼이 하단 상담바 밑으로 들어가는 구조였고, 스크롤도 안 생겨 없는 것처럼 보였다. 짧은 폰(SE·미니급)에서만 나타나 눈에 안 띄었다. (`e96dde7`)
- **마크업이 스스로 높이를 만들게 전환** — 상수를 없애고 모자란 분야는 빈 줄로 채운 뒤 줄마다 `h-14` 로 못 박았다. 행 여백을 `py-2.5`→`py-2` 로 줄인 것은 두 줄 제목이 섞일 때만 행이 커지던 원인을 없애기 위함이다. 격자 줄을 `minmax(0, 1fr)` 로 풀어 남는 높이를 목록에 내주게 하고, 780px 이하에서는 목록을 두 건만 남겼다(누르는 것은 분야 칸이라 줄이는 쪽은 목록이 먼저다). `components/home/case-fields-tabs.tsx`·`app/globals.css` (`e96dde7`)

### 5. 홈페이지 V2 — 지사 페이지 내부 링크 (이슈 `#91`)

- **🔴 내부 링크가 사이트 전체에 0개였다** — `/seoul`·`/busan` 같은 지사 URL 로 가는 내부 링크가 **한 개도 없다**는 것을 실측했다. 지역 선택이 쿠키 기반이라 URL 이 화면에 드러나지 않고 사무소 지도도 전화·네이버지도 링크만 갖고 있어, 지사 페이지 10곳이 **사이트맵에만 존재**하는 상태였다. 사이트맵에만 있으면 '존재 신고' 는 되지만 '중요도 신고' 가 안 되고, 구글은 사이트링크를 **내부 링크 구조에서** 뽑기 때문에 후보에 오르지 못했다(레거시는 홈에 지사 링크 7개가 있어 그 목록이 뜬다). (`507308f`)
- **푸터를 택한 이유** — 전 페이지에 걸려 있어 한 곳만 고쳐도 신호가 가장 크고 지사가 늘면 자동으로 붙는다. 디자인 변화는 지사명이 클릭되고 hover 에 밑줄이 생기는 것뿐이다. `regionHref` 를 쓰지 않은 것은 그 함수가 **현재 보고 있는 지역을 접두사로 붙이기** 때문 — 여기는 각 지사 자신의 주소로 가야 한다. `components/layout/site-footer.tsx` (`507308f`)

### 6. 홈페이지 V2 — 전환추적 계측 감사 (이슈 `#84`)

- **컨테이너 15개 전수 파싱** — 감사 스크립트(`docs/gtm-audit.mjs`)를 확장해 게시본 `gtm.js` 의 `tags`/`rules`/`predicates`/`macros` 를 전수 파싱했다. 판정 기준은 "V2 에서 그 태그의 발화 조건이 성립하는가" 하나다. **운영 조치** (`#84`)
- **🔴 GA4 상담 접수 전환이 전 지사 0 — 신규 발견** — V2 가 쏘는 `consult_submit`·`{지사}_상담접수` 를 수신하는 GA4 태그가 **13개 컨테이너에 하나도 없다.** 수원·천안·대전·청주·남양주·부산은 `gtm.formSubmit` 을 기다리는데 V2 는 fetch 제출이라 그 이벤트가 발생하지 않고, 서울·부천·인천·광주는 레거시 이벤트명을 기다리는데 V2 는 `{지사}_` 접두사가 붙어 불일치다. 네이버는 코드 직접 전송으로 살렸지만 **GA4 는 GTM 중계 구조라 콘솔 작업이 남아 있었다.** (`#84`)
- **선택자 기반 사망 / URL 기반 생존, 네이버 예외 정정** — CSS 선택자에 묶인 GA4 태그 20여 개는 V2 마크업에서 성립하지 않지만, URL 조건 태그(카톡·톡톡·예약·대표전화)는 링크 주소가 레거시와 같아 **정상 발화**한다. 네이버 예외는 기존 기록의 "서울·광주만"이 부천을 빠뜨린 것이었다(서울 8/8 · 광주 6/6 · **부천 4/4**, 미처리 29개). (`#84`)
- **PC 대표전화 전환 복원 + 정정 2건** — 전환이 잡히던 경로가 **모바일 하단 독 하나뿐**이었고 헤더 버튼·메가패널 전화 바는 발신해도 아무것도 쏘지 않았다. `CallPopover` 는 내부 5군데에서 이미 `onDial` 을 호출하므로 **누락은 컴포넌트가 아니라 호출부에 있었고**, prop 하나씩으로 모든 발신 경로를 덮었다. 정정 ① 그 자리는 모바일 GNB 가 아니라 **데스크탑 메가패널**이라 영향 범위가 PC 전량이었다 ② **푸터 `tel:` 은 붙이면 안 된다** — 레거시 js 16개 전수 확인 결과 푸터(`.tel_click`)를 전환으로 잡은 지사는 **수원 하나뿐**이고 헤더(`.emer`)는 13개 지사가 쏘고 있었다. 즉 이번 복원은 레거시 동작 복귀지만, 푸터에 붙이면 15개 지사에서 없던 전환이 생겨 수치가 부푼다. `header-call-button.tsx`·`site-nav.tsx` (`2048bcf`)
- **GA4 콘솔 관문 5건 신규 제기** — 감사가 "태그가 발화하는가" 까지라는 한계를 짚고 그 뒤 관문을 정리했다: 데이터 보존 기간(기본 2개월, 지나간 건 복구 불가) · 키 이벤트 표시(안 하면 전환 리포트는 여전히 0) · 내부 트래픽 제외 · DebugView 도착 검증 · 네이버 전환 스크립트 어시스턴트. **GA4↔구글Ads 연결은 지금 하면 안 된다**(자체 태그와 이중 집계)는 경고도 남겼다. (`#84`)

### 7. 홈페이지 V2 — 미해결 이슈 전수 점검 (커밋 없음)

- **종결 3건** — ① 전문센터 전화 버튼(`#56`): 라이브에서 `tel:` 링크 존재를 확인해 3개 항목 전부 닫음 ② 레거시 404 URL 301 매핑(`#114`): 대표 5건 재실측 전부 301 정상, 확장자 포함·미포함 양쪽 경로가 다 잡히는 것까지 확인 ③ 모바일 검색 팝업(`#119`): 배포 반영 확인, 남은 "실기기 확인" 2건은 회귀 점검 성격이라 이 이슈로 열어 둘 항목이 아님을 명시. **운영 조치**
- **🔴 지금도 진행 중인 유실 확인**(`#81`) — 하단 상담바에서 '회생·파산' 을 선택해 접수해도 **분기가 코드에 존재하지 않는다**(`ONLINE_CONSULT_MAININDEX = 2` 고정). 지사 문의로 저장돼 회생팀 어드민 화면에 안 뜨고, `centerindex=0` 이라 회생 구글챗 웹훅도 안 울리며, 회생 사건을 진행하지 않는 접수 지사의 문의함에만 남는다. 8/6 등록 시점과 동일한 상태다. **운영 조치**
- **어드민 체감 버그의 정체 규명**(`#82`) — `next.config.ts` 에 `cacheHandler` 설정이 없어 인스턴스마다 ISR/fetch 캐시가 따로 돈다. 어드민 수정 후 인스턴스에 따라 예전 값이 계속 응답되고(약 5분간 순차 수렴), 새로고침하면 바뀌기도 해서 **재현 안 되는 버그로 오인**되기 쉽다. 오늘 발견한 이미지 변환 캐시와 같은 뿌리다. **운영 조치**
- **성격 재분류·결정 대기 정리** — 상담 캡(`#109`)은 캡 자체가 8/18 배포로 들어갔으므로 제목은 달성됐고 남은 **Redis 공유 저장소**만 별도 이슈로 떼는 것이 맞다(묶여 있으면 "캡이 없다"로 오독된다). CRM(`#99`)은 코드·설정이 끝나 **개발 몫이 없다**. 그 밖에 값·방침만 정하면 반영이 짧은 5건을 정리했다 — 사무소 이미지(`#3`, 원본 과대 `daejeon_office.jpg` 9.7MB 발견) · 지식센터 CTA(`#27`) · 자석 모션 강도(`#29`) · 썸네일 크롭(`#30`) · 연락처 폴백(`#71`, 지금 위험 0 이나 신규 지사가 열리면 서울 예약 캘린더에 다른 지사 예약이 잡힌다). **운영 조치**

---

**금일 종합:** 마케팅 요청에서 출발한 문구 개편이 "브랜드명만 있는 제목"이라는 구조적 결함으로 이어졌고, 그것을 고치다 지사 문구까지 손댔다가 **요청 범위를 넘어선 판단이었음을 스스로 인지해 9분 만에 되돌린** 것이 오늘의 성격을 잘 보여준다 — 코드로 할 수 있다는 것과 코드로 해야 한다는 것은 다르고, 지사 문구는 마케팅·대행사 검수를 거쳐 어드민에 들어가는 것이 정상 경로다. 같은 판단이 변호사 카드에서도 반복됐다: 자동 선별 알고리즘을 만들어 놓고도 **어드민에서 사람이 고쳐도 규칙이 이긴다**는 이유로 폐기하고, 사람이 이미 추려 둔 칸을 입력 순서 그대로 쓰는 쪽을 택했다. 기술적으로는 홈 첫 화면 지연을 발견 지연과 원본 형식 두 겹으로 분리해 각각 조치하면서 **Cloud Run 인스턴스 로컬 캐시**라는 공통 뿌리를 찾아 기존 ISR 이슈와 묶은 것이 수확이고, 계측 쪽에서는 GA4 상담 접수 전환이 전 지사 0 이라는 구멍을 새로 찾고 PC 대표전화 전환을 복원하면서 **푸터에는 붙이면 안 된다**는 것까지 레거시 실측으로 가려냈다. 오후에는 미해결 이슈 11건을 전수 점검해 3건을 종결하고, 나머지는 "개발 몫 / 결정 대기 / 운영 협의"로 성격을 갈라 다음 사람이 바로 집을 수 있게 정리했다. 금일 1개 repo에 커밋 10건, 이슈 코멘트 22건.
