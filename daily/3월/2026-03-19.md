# 일일 업무 보고 (2026. 03. 19)

> CRM (고객관계관리 시스템) — 금일 커밋 12건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **조직키 기반 연락처 저장**: 로그인 계정 조직키 기준으로 모바일 공용 연락처 저장 대상 제한·기본 선택 보정, 허용된 공용 계정만 표시
- **인증에 조직키 포함**: 로그인 응답에 조직키 정보 포함, 프론트 조직키 캐시 저장 및 로그아웃 정리
- **직원 상세 보조 이메일**: 직원 상세 다이얼로그에 보조 이메일 표시(쉼표 기준 개행)
- **당사자 목록**: 필터 기본값·유지 동작 개선

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### 🔐 조직키 기반 공용 연락처 저장

**조직키 기준 저장 대상 제한** — `backend/src/controllers/googleContactController.js`, `backend/src/routes/party.js`
- 조직키를 기준으로 모바일 공용 연락처 저장 대상을 제한하고(`cb5b49c`), 저장 대상과 수동 선택 로직을 분리(`e5b63bf`), 로그인 계정 조직키 기반 공용 연락처 기본 선택을 보정. (`92b3b8f`)

**프론트 저장 다이얼로그 보정** — `frontend/lib/pages/party_detail_screen.dart`, `frontend/lib/widgets/contact_sync_dialog.dart`
- 연락처 저장 계정을 허용된 공용 계정만 표시하도록 하고(`a8dbf15`), 공용 계정이 없어도 저장 다이얼로그가 표시되게 하며(`739a574`), 검색 목록에 전체 직원을 복원(`8168b65`), 기본 선택 응답 키 파싱을 보정. (`4fff636`)

#### 🔑 인증에 조직키 포함

**로그인 응답 조직키** — `backend/src/controllers/authController.js`, `googleContactController.js`, `backend/src/utils/currentUserEmployee.js`
- 로그인 응답에 조직키 정보를 포함하도록 개선. (`74f248c`)

**프론트 조직키 캐시** — `frontend/lib/layouts/main_layout.dart`, `login_page.dart`
- 조직키를 캐시에 저장하고 로그아웃 시 정리하는 처리를 추가. (`a54e6dd`)

#### 👤 직원 상세 보조 이메일

`backend/src/controllers/employeeController.js`, `frontend/lib/pages/staff_screen.dart`
- 직원 상세 다이얼로그에 보조 이메일 표시를 추가하고(`775272f`), 쉼표 기준 개행으로 가독성을 개선. (`494502f`)

#### 📋 당사자 목록

`frontend/lib/pages/party_screen.dart`
- 당사자 목록 필터의 기본값과 유지 동작을 개선. (`71fabe9`)

---

**금일 종합:** 공용 연락처 저장을 로그인 계정의 조직키 기준으로 제한·기본선택 보정하고, 로그인 응답·프론트 캐시에 조직키를 반영해 일관된 권한 흐름을 구성함. 직원 상세 보조 이메일 표시와 당사자 목록 필터 동작도 함께 개선함.
