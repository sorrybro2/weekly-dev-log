# 일일 업무 보고 (2026. 03. 06)

> CRM (고객관계관리 시스템) — 금일 커밋 12건 기준

## ⏱️ 10초 핵심 요약

**[CRM (고객관계관리 시스템)]**
- **문자 이미지 첨부(MMS)**: 문자 이미지 GCS 업로드 및 MMS 발송 지원, 발송/양식 화면 이미지 첨부 UI 추가
- **이미지 업로드 안정화**: PNG→JPEG 변환, 이미지 비율 유지·자동 최적화, GCS 인증 폴백·프록시 URL, octet-stream 허용, 업로드 실패 응답 파싱 안정화
- **예약 발송 UI**: 발송 시각을 년/월/일·시/분으로 완전 분리 및 입력 방식 개선
- **문자 기본값**: 문자 제목을 '법무법인 법승'으로 설정
- **배포**: 컨테이너 시작 시 sms-image-url 마이그레이션 자동 실행

---

## 📝 상세 업무 내역

### 1. CRM (고객관계관리 시스템)

#### 🖼️ 문자 이미지 첨부(MMS) 기능

**문자 이미지 GCS 업로드 및 MMS 발송** — `backend/migrations/add_sms_image_url_columns.sql`, `backend/src/controllers/partySmsCallController.js`, `smsTemplateController.js`, `backend/src/routes/upload.js`, `backend/src/utils/gcsUpload.js`, `ncpSms.js`
- 문자 이미지를 GCS에 업로드하고 MMS로 발송하는 기능을 추가(이미지 URL 컬럼 마이그레이션 포함). (`43e0c45`)
- 문자 발송/양식 화면에 이미지 첨부 UI를 추가(관련 Flutter 플러그인 반영). (`a3974ab`)

**이미지 처리 안정화** — `backend/src/routes/upload.js`, `backend/src/utils/gcsUpload.js`, `ncpSms.js`, `frontend/lib/pages/sms_form_screen.dart`
- NCP MMS 파일 업로드를 위해 PNG 이미지를 JPEG로 변환. (`ccd7fc9`)
- MMS 이미지의 비율을 유지하며 자동 최적화해 업로드하도록 처리. (`0d1fba6`)
- GCS 인증 폴백 및 이미지 프록시 URL 반환을 추가하고(`7fa790f`), octet-stream 형식 이미지 업로드를 허용. (`e7eaa1e`)
- 업로드 실패 시 응답 파싱을 안정화. (`fada320`)

#### 🕐 예약 발송 UI 개선

`frontend/lib/pages/sms_form_screen.dart`
- 예약 발송 시각을 시/분으로 분리하고 입력 방식을 개선(`b39ef72`, `484bc83`), 이어서 날짜(년·월·일)와 시간(시·분)을 완전히 분리. (`af5f33d`)

#### ⚙️ 기본값·배포

- **문자 제목 기본값** — `backend/src/utils/ncpSms.js`: 문자 제목을 '법무법인 법승'으로 설정. (`0531218`)
- **마이그레이션 자동 실행** — `backend/Dockerfile`, `backend/package.json`: 컨테이너 시작 시 sms-image-url 마이그레이션이 실행되도록 배포 구성을 조정. (`e4ce078`)
- **문서** — `README.md`: GCS 문자 이미지 발송 저장 관련 문서 정리. (`f248bd0`)

---

**금일 종합:** 문자 이미지 첨부(MMS) 기능을 도입해 GCS 업로드·발송 경로를 구축하고, PNG→JPEG 변환·비율 유지·인증 폴백·octet-stream 허용 등 업로드 안정화를 폭넓게 처리함. 예약 발송 UI의 날짜/시간 분리와 배포 시 마이그레이션 자동 실행까지 반영함.
